[Unit]
Description=Gootodo daemon
After=network.target

[Service]
ExecStart=/usr/local/bin/gootodo daemon
WorkingDirectory=/usr/local/bin/
StandardOutput=journal
StandardError=journal
Restart=always

[Install]
WantedBy=multi-user.target
